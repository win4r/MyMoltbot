# Moltbot Todo

一个基础但完整的 Todo List 应用：新增 / 完成 / 编辑 / 删除 / 筛选 / 清除已完成，数据使用 LocalStorage 本地保存。

## 本地开发

```bash
npm i
npm run dev -p 18790 -H 0.0.0.0
```

然后打开：
- http://localhost:18790

> 说明：在 Codespace 场景下，使用对应端口的 `*.app.github.dev` 公网地址访问。
